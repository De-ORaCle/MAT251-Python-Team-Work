
import math

# Collect input from the user
x = float(input("Enter a value for x: "))

# Calculate the exponential function f(x) = e^x
result = math.exp(x)

# Print the result
print(f"f(x) = {result}")
